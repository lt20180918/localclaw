# Web Panel 端口
PORT=8080

# JWT 密钥 (生产环境请更换)
JWT_SECRET=change-me-in-production

# 管理员密码 (首次启动后会被 bcrypt 加密存入 SQLite)
ADMIN_PASSWORD=admin123

# OpenClaw Gateway 地址
OPENCLAW_GATEWAY_URL=ws://127.0.0.1:18789

# OpenClaw Gateway Token (可选, 本地连接自动放行)
OPENCLAW_GATEWAY_TOKEN=
