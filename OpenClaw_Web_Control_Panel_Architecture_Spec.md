# OpenClaw Web Control Panel

## 架构设计 + 实现说明文档（v2 — 已实施）

> 本文档反映当前已实现的系统架构，最后更新：2026-02-18

---

## 一、项目定位

OpenClaw Web Control Panel 是一个部署在无头局域网服务器上的 Web 控制面板，用于管理 OpenClaw Agent、进行 AI 对话、查看状态与日志，并支持独立升级。

**核心特点：**

- 无头服务器部署（无需显示器/键盘）
- 局域网多端访问 `http://ip:8080`
- 响应式自适应 UI（手机 / 平板 / 桌面）
- 不侵入 OpenClaw 本体（通过 Gateway WS-RPC 通信）
- OpenClaw 与 Web Panel 独立升级
- **小白友好**：首次使用引导向导，只需填 API Key

---

## 二、总体架构

```
浏览器 (手机/电脑)
    │
    │  HTTP + WebSocket
    ▼
┌─────────────────────────────┐
│  Web Control Panel (:8080)  │
│  ┌────────┐  ┌────────────┐ │
│  │ React  │  │ Express.js │ │
│  │ SPA    │  │ REST API   │ │
│  │ (Vite) │  │ + WS Bridge│ │
│  └────────┘  └─────┬──────┘ │
│                    │        │
│            WS-RPC  │        │
│                    ▼        │
│          ┌─────────────┐    │
│          │ GatewayClient│    │
│          └──────┬──────┘    │
└─────────────────┼───────────┘
                  │
                  ▼
        ┌──────────────────┐
        │ OpenClaw Gateway │
        │   (:18789)       │
        │  ┌────────────┐  │
        │  │ OpenClaw    │  │
        │  │ Core        │  │
        │  └────────────┘  │
        └──────────────────┘
```

**分层说明：**

| 层级 | 技术栈 | 说明 |
|------|--------|------|
| 前端 SPA | React 19 + TypeScript + Vite | 深色主题，响应式 |
| 后端 API | Express.js + TypeScript | REST + WebSocket |
| 数据持久化 | sql.js (SQLite) | 纯 JS，无 node-gyp |
| Gateway 通信 | WebSocket JSON-RPC | 自动重连 |
| 容器化 | Docker (ARM64) | Compose 一键部署 |

---

## 三、技术栈

### 后端

| 组件 | 选型 | 说明 |
|------|------|------|
| 运行时 | Node.js 22 | LTS |
| 框架 | Express 5 | REST API |
| 语言 | TypeScript 5 | 严格模式 |
| 认证 | jsonwebtoken | JWT |
| 数据库 | sql.js | 纯 JS SQLite, 无原生依赖 |
| 密码哈希 | bcryptjs | 纯 JS 实现 |
| WebSocket | ws | 客户端 + 服务端 |
| 限流 | express-rate-limit | 请求频率控制 |

### 前端

| 组件 | 选型 | 说明 |
|------|------|------|
| 框架 | React 19 | Hooks |
| 构建 | Vite 6 | HMR 开发 |
| 语言 | TypeScript 5 | |
| 样式 | 纯 CSS | 自定义设计系统, 无框架 |
| Markdown | 内置渲染器 | MessageBubble 组件内实现 |

---

## 四、项目目录结构

```
localclaw/
├── .env.example              # 环境变量模板
├── docker-compose.yml        # Docker Compose 部署
├── Dockerfile                # 多阶段构建 (ARM64)
├── docs/
│   ├── DEPLOY.md             # 部署指南
│   └── DEVELOPMENT.md        # 开发指南
│
├── server/                   # 后端
│   ├── package.json
│   ├── tsconfig.json
│   └── src/
│       ├── index.ts          # 入口 (启动服务)
│       ├── app.ts            # Express 应用 + 中间件注册
│       ├── config.ts         # 配置 (.env 读取)
│       ├── db/
│       │   └── database.ts   # SQLite 初始化 (sql.js)
│       ├── middleware/
│       │   ├── auth.ts       # JWT 认证中间件
│       │   ├── rateLimiter.ts# 请求限流
│       │   └── websocket.ts  # WebSocket 服务 + Chat 桥接
│       ├── routes/
│       │   ├── auth.ts       # POST /api/login, /verify, /change-password
│       │   ├── status.ts     # GET /api/status
│       │   ├── openclaw.ts   # Gateway 代理 (config, agents, logs, update)
│       │   └── setup.ts      # Setup Wizard API (status, config, save, test)
│       ├── services/
│       │   ├── authService.ts    # 密码哈希 + 验证
│       │   ├── gatewayClient.ts  # OpenClaw Gateway WS-RPC 客户端
│       │   ├── chatBridge.ts     # WebSocket Chat 桥接
│       │   └── setupService.ts   # 本地设置管理 (SQLite)
│       └── types/
│           ├── express.d.ts
│           └── sql.js.d.ts
│
├── client/                   # 前端
│   ├── package.json
│   ├── tsconfig.json
│   ├── vite.config.ts        # Vite 配置 + API 代理
│   └── src/
│       ├── main.tsx          # React 入口
│       ├── App.tsx           # 应用根 (路由 + Setup Wizard 集成)
│       ├── index.css         # 全局样式 (设计系统 + 响应式)
│       ├── api/
│       │   └── client.ts     # 统一 API 请求封装
│       ├── context/
│       │   └── AuthContext.tsx# 认证上下文 (登录/登出/Token)
│       ├── hooks/
│       │   └── useWebSocket.ts# WebSocket Hook
│       ├── components/
│       │   ├── SetupWizard.tsx   # 首次使用引导 (4步)
│       │   ├── Sidebar.tsx       # 侧边栏导航
│       │   ├── StatusPanel.tsx   # 系统状态面板
│       │   ├── ChatWindow.tsx    # 对话窗口 (WS 流式)
│       │   ├── ChatInput.tsx     # 输入框 + 发送/中止
│       │   └── MessageBubble.tsx # 消息气泡 + Markdown
│       └── pages/
│           ├── LoginPage.tsx     # 登录页
│           ├── DashboardPage.tsx # 仪表盘
│           ├── ChatPage.tsx      # 对话页
│           ├── ConfigPage.tsx    # 配置管理 (表单化)
│           └── SettingsPage.tsx  # 系统设置 (改密码)
│
└── data/                     # 运行时数据 (git ignored)
    └── localclaw.db          # SQLite 数据库
```

---

## 五、核心功能

### 5.1 登录与认证

| 端点 | 方法 | 说明 |
|------|------|------|
| `/api/login` | POST | 密码登录, 返回 JWT |
| `/api/verify` | GET | Token 校验 |
| `/api/change-password` | POST | 修改管理密码 |

- 单管理员密码 (bcrypt 哈希, 存 SQLite)
- JWT Token (默认 24h 过期)
- 所有 API (除 login) 需 `Authorization: Bearer <token>`
- 请求限流: 100 次/分钟

### 5.2 Setup Wizard（首次引导）

**用户流程：**

```
登录 → 检测是否配置 → 未配置 → 弹出向导 → 填 API Key → 保存+测试 → 跳转对话
```

| 端点 | 方法 | 说明 |
|------|------|------|
| `/api/setup/status` | GET | 是否已完成初始设置 |
| `/api/setup/config` | GET | 获取配置 (Key 脱敏, 仅末 4 位) |
| `/api/setup/save` | POST | 保存配置 + 推送 Gateway |
| `/api/setup/test` | POST | 测试 API Key 有效性 |

**可配置项：**

| 字段 | 说明 | 必填 |
|------|------|------|
| `llm_api_key` | LLM 服务 API Key | ✅ |
| `llm_model` | 模型选择 (gpt-4o 等) | ❌ |
| `llm_base_url` | 自定义 API 地址 | ❌ |
| `channel_appid` | 渠道 App ID | ❌ |
| `channel_secret` | 渠道密钥 | ❌ |

- 配置持久化到 SQLite `settings` 表
- 保存时同步推送到 Gateway (`config.set` RPC)
- API Key 在 API 响应中自动脱敏

### 5.3 系统状态

| 端点 | 方法 | 说明 |
|------|------|------|
| `/api/status` | GET | Web 版本 + Gateway 连接状态 |

返回示例：
```json
{
  "code": 0,
  "data": {
    "webVersion": "0.1.0",
    "gateway": {
      "connected": true,
      "gatewayVersion": "2026.x",
      "status": { ... },
      "health": { ... }
    }
  }
}
```

- 启动时自动连接 Gateway (自动重连, 5s 间隔)
- 前端 30s 轮询刷新
- Gateway 未连接时显示连接指引

### 5.4 对话功能（WebSocket）

| 路径 | 协议 | 说明 |
|------|------|------|
| `/ws` | WebSocket | 对话 + 事件流 |

**消息类型：**

| 类型 | 方向 | 说明 |
|------|------|------|
| `chat` | 客户端→服务端 | 发送对话 |
| `abort` | 客户端→服务端 | 中止回复 |
| `chat.token` | 服务端→客户端 | 流式 Token |
| `chat.done` | 服务端→客户端 | 回复完成 |
| `gateway.status` | 服务端→客户端 | 连接状态变更 |

- JWT Token 通过 WebSocket URL 传递: `ws://host/ws?token=xxx`
- 流式渲染 + 自动滚动
- 内置 Markdown 渲染 (代码高亮 + 行内代码 + 列表 + 标题)
- 支持发送中止

### 5.5 OpenClaw 管理 (Gateway 代理)

| 端点 | 方法 | 说明 |
|------|------|------|
| `/api/config` | GET | 获取 OpenClaw 配置 |
| `/api/config` | PUT | 更新 OpenClaw 配置 |
| `/api/agents` | GET | 获取 Agent 列表 |
| `/api/logs` | GET | 获取日志 |
| `/api/openclaw/update` | POST | 触发 OpenClaw 升级 |

- 通过 Gateway WS-RPC 代理
- Gateway 未连接时返回 `50301` 错误码

---

## 六、Gateway 通信

Web Panel 通过 `GatewayClient` 与 OpenClaw Gateway 通信：

```
Web Panel  ──WS──>  Gateway (:18789)
              │
              ├── RPC 调用 (带 id, 等待响应)
              │   status, health, models.list
              │   sessions.list, config.get, config.set
              │   chat.history
              │
              └── 消息发送 (无需等待)
                  chat.send, chat.abort
```

**WS-RPC 协议：**

```json
// 请求
{ "id": "1", "method": "config.get", "params": {} }

// 响应
{ "id": "1", "payload": { ... } }

// 事件推送 (无 id)
{ "type": "chat.token", "content": "Hello" }
```

- 30s RPC 超时
- 断线 5s 自动重连
- 支持 Authorization Header

---

## 七、数据持久化

使用 sql.js (纯 JavaScript SQLite 实现, 无原生依赖)。

**数据库表：**

| 表 | 用途 |
|------|------|
| `auth` | 管理员密码 (bcrypt 哈希) |
| `settings` | 用户配置 (key/value: API Key、Model 等) |
| `chat_history` | 对话记录 (role + content + session_key) |

数据库文件: `data/localclaw.db`

---

## 八、安全机制

| 功能 | 实现 |
|------|------|
| 认证 | JWT Token (HS256) |
| 密码存储 | bcrypt 哈希 (10 rounds) |
| 请求限流 | 100 次/分钟/IP |
| 访问日志 | Express 中间件输出 |
| API Key 保护 | 响应脱敏 (仅显示末 4 位) |
| CORS | 已启用 |

---

## 九、响应式 UI

**断点设计：**

| 断点 | 布局 |
|------|------|
| < 768px | 单栏, 侧边栏折叠为 Drawer |
| ≥ 768px | 侧边栏 + 内容区 |

**页面结构：**
```
┌──────┬────────────────┐
│      │  Header [退出]  │
│ Side │────────────────│
│ bar  │                │
│      │   Page Content │
│ 🦞   │                │
│ 📊   │                │
│ 💬   │                │
│ 🔧   │                │
│ ⚙️   │                │
└──────┴────────────────┘
```

**已实现：**

- ✅ Drawer 侧边栏 (手机端点击 ☰ 弹出)
- ✅ 固定底部输入框
- ✅ Markdown 渲染 (标题/列表/代码块/行内代码/粗体/斜体)
- ✅ 代码块横向滚动
- ✅ 深色主题 (Glassmorphism 效果)
- ✅ CSS 变量设计系统
- ✅ 动画 (fadeIn / slideUp / 流式光标)
- ✅ 100dvh 全屏

---

## 十、部署

### Docker (推荐)

```yaml
# docker-compose.yml
services:
  web-panel:
    build: .
    ports:
      - "8080:8080"
    environment:
      - ADMIN_PASSWORD=你的密码
      - JWT_SECRET=随机密钥
      - OPENCLAW_GATEWAY_URL=ws://openclaw:18789
    volumes:
      - panel-data:/app/data
    restart: unless-stopped

volumes:
  panel-data:
```

Dockerfile 采用多阶段构建，支持 ARM64 (Ubuntu Server)。

### 手动部署

```bash
# 安装依赖
cd server && npm install && cd ../client && npm install

# 构建前端
cd client && npm run build

# 启动服务
cd server && npm start
# 访问 http://localhost:8080
```

---

## 十一、环境变量

| 变量 | 默认值 | 说明 |
|------|--------|------|
| `PORT` | 8080 | Web Panel 端口 |
| `ADMIN_PASSWORD` | admin123 | 管理员密码 |
| `JWT_SECRET` | dev-secret-change-me | JWT 签名密钥 |
| `JWT_EXPIRES_IN` | 86400 | Token 过期 (秒) |
| `OPENCLAW_GATEWAY_URL` | ws://127.0.0.1:18789 | Gateway 地址 |
| `OPENCLAW_GATEWAY_TOKEN` | (空) | Gateway 认证 Token |
| `DB_PATH` | data/localclaw.db | 数据库路径 |
| `RATE_LIMIT_MAX` | 100 | 限流: 最大请求数 |
| `RATE_LIMIT_WINDOW_MS` | 60000 | 限流窗口 (毫秒) |

---

## 结束

本系统目标：

在手机或电脑浏览器输入 `http://192.168.x.x:8080`，登录后填入 API Key 即可管理 OpenClaw，**无需 SSH 或 CLI**。
